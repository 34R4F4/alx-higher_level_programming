#!/usr/bin/node
/* script prints 3 lines, but by using an array of string and a loop */
const myArr = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

myArr.forEach(elmnt => console.log(elmnt));
